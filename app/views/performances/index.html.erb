<p id="notice"><%= notice %></p>

<!--<h1>Performances</h1>
<div>
  <h4>Import data!</h4>
  <%= form_tag import_performances_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag "Import from file"%>
  <% end %>
</div>-->

<table>
  <thead>
    <tr>
      <th>event_id</th>
      <th>date</th>
      <th>work_id</th>
      <th>title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @performances.each do |performance| %>
      <tr>
        <td><%= performance.event_id %></td>
        <td><%= performance.event.date %></td>
        <td><%= performance.work_id %></td>
        <td><%= performance.work.title %></td>
        <td><%= link_to 'Show', performance %></td>
        <td><%= link_to 'Edit', edit_performance_path(performance) %></td>
        <td><%= link_to 'Destroy', performance, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Performance', new_performance_path %>
